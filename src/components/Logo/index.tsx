import { LogoIcon, LogoWrapper } from './styles';

const Logo = () => {
  return (
    <div>
      <LogoWrapper>
        <LogoIcon />
        <p>Dashboard Name</p>
      </LogoWrapper>
    </div>
  );
};

export default Logo;
