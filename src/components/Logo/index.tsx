import { LogoIcon, LogoWrapper } from './styles';

const Logo = () => {
  return (
    <div>
      <LogoWrapper>
        <LogoIcon />
        <p>Security Report</p>
      </LogoWrapper>
    </div>
  );
};

export default Logo;
